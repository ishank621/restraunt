npm install mongoose@5.1.7 mongoose-currency@0.2.0 --save 
mongoose currency os for currency
intall bluebird
npm install express-session@1.15.6 session-file-store@1.2.0 --save
npm install passport@0.4.0 passport-local@1.0.0 passport-local-mongoose@5.0.1 --save
npm install passport-jwt@4.0.0 jsonwebtoken@8.3.0 --save



to authenticate using https
1. cd bin (inside restraunt server)
2. openssl genrsa 1024 > private.key
3. openssl req -new -key private.key -out cert.csr
4. openssl x509 -req -in cert.csr -signkey private.key -out certificate.pem
5.
